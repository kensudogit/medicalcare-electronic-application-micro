{
  "name": "medicalcare-electronic-application-micro",
  "version": "1.0.0",
  "description": "Medical Care Electronic Application - Microservices Architecture",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && echo 'Build completed' > public/index.html",
    "start": "echo 'This project uses Docker Compose for backend services. Use docker-compose up -d to start services.' && exit 0",
    "deploy": "vercel --prod",
    "local": "node -e \"console.log('Local development server ready')\"",
    "test": "echo 'Testing API endpoints'"
  },
  "keywords": ["medical", "microservices", "api", "vercel", "serverless"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^46.0.2"
  }
}
